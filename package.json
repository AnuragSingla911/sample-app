{
  "name": "sample-app-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end (Playwright) tests for the sample full-stack app",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "1.44.1"
  }
}
